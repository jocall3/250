import React, { useReducer, useMemo } from 'react'; import { ThemeContext } from './ThemeContext'; import { themeReducer, initialThemeState } from './ThemeReducer'; import { ThemeProviderProps } from './ThemeTypes'; export const ThemeProvider: React.FC<ThemeProviderProps> = ({ children }) => { const [state, dispatch] = useReducer(themeReducer, initialThemeState); const value = useMemo(() => ({ state, dispatch }), [state]); return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>; };