import { ITransferRule, TransferContext } from '../../types'; export class GeographicRestrictionRule implements ITransferRule { private readonly RESTRICTED_COUNTRIES = ['KP', 'IR', 'SY', 'CU']; async evaluate(context: TransferContext): Promise<boolean> { if (this.RESTRICTED_COUNTRIES.includes(context.receiver.countryCode)) { throw new Error('Transfers to this jurisdiction are restricted.'); } return true; } }