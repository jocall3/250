import { RuleSet } from './RuleSet'; import { RuleContext } from './RuleContext'; import { RuleResult } from './RuleResult'; export class RuleEvaluator { async evaluate(ruleSet: RuleSet, context: RuleContext): Promise<RuleResult[]> { const results: RuleResult[] = []; for (const rule of ruleSet.rules) { const result = await rule.evaluate(context); results.push(result); if (!result.success) break; } return results; } }