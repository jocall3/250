import { ThemeState, ThemeAction } from './ThemeTypes'; export const initialThemeState: ThemeState = { mode: 'light', primaryColor: '#FFD700' }; export const themeReducer = (state: ThemeState, action: ThemeAction): ThemeState => { switch (action.type) { case 'TOGGLE_MODE': return { ...state, mode: state.mode === 'light' ? 'dark' : 'light' }; case 'SET_PRIMARY_COLOR': return { ...state, primaryColor: action.payload }; default: return state; } };