import React, { useRef } from 'react'; import { Canvas, useFrame } from '@react-three/fiber'; import { OrbitControls } from '@react-three/drei'; import { LightSourceSimulator } from './LightSourceSimulator'; export const ThreeDViewer = ({ texture, lightMode, tilt }: { texture: any, lightMode: string, tilt: {x: number, y: number} }) => { const meshRef = useRef<any>(null); useFrame(() => { if (meshRef.current && tilt) { meshRef.current.rotation.x = tilt.x; meshRef.current.rotation.y = tilt.y; } }); return (<Canvas camera={{ position: [0, 0, 5] }}><LightSourceSimulator mode={lightMode} /><mesh ref={meshRef}><boxGeometry args={[5, 2.14, 0.01]} /><meshStandardMaterial map={texture} roughness={0.4} metalness={0.1} /></mesh><OrbitControls enableZoom={true} enablePan={false} /></Canvas>); };