export enum SettlementStatus { PENDING, COMPLETED, FAILED } export class Settlement { constructor(public readonly id: string, public readonly listingId: string, public readonly bidId: string, public readonly escrowId: string, public status: SettlementStatus = SettlementStatus.PENDING) {} public process(): void { if (this.status !== SettlementStatus.PENDING) throw new Error('Settlement already processed or failed.'); this.status = SettlementStatus.COMPLETED; } public fail(): void { this.status = SettlementStatus.FAILED; } }