export class TiltController { static handleMouseMove(event: MouseEvent, setTilt: (tilt: {x: number, y: number}) => void) { const x = (event.clientX / window.innerWidth) * 2 - 1; const y = -(event.clientY / window.innerHeight) * 2 + 1; setTilt({ x: y * 0.2, y: x * 0.2 }); } static handleDeviceOrientation(event: DeviceOrientationEvent, setTilt: (tilt: {x: number, y: number}) => void) { const beta = event.beta || 0; const gamma = event.gamma || 0; setTilt({ x: (beta - 45) * 0.01, y: gamma * 0.01 }); } }