import { ITransferRule, TransferContext } from '../../types'; export class MultiSigApprovalRule implements ITransferRule { private readonly HIGH_VALUE_THRESHOLD = 10000; async evaluate(context: TransferContext): Promise<boolean> { if (context.bill.value >= this.HIGH_VALUE_THRESHOLD) { if (!context.signatures || context.signatures.length < 2) { throw new Error('Multiple signatures required for high-value transfers.'); } } return true; } }