export enum ListingStatus { ACTIVE, SOLD, CANCELLED } export class Listing { constructor(public readonly id: string, public readonly sellerId: string, public readonly billId: string, public price: number, public status: ListingStatus = ListingStatus.ACTIVE) {} public cancel(): void { if (this.status !== ListingStatus.ACTIVE) throw new Error('Only active listings can be cancelled.'); this.status = ListingStatus.CANCELLED; } public markAsSold(): void { if (this.status !== ListingStatus.ACTIVE) throw new Error('Only active listings can be sold.'); this.status = ListingStatus.SOLD; } }